# Backend dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Marker-PDF core dependencies
Pillow>=10.1.0
pydantic>=2.4.2
pydantic-settings>=2.0.3
# torch is installed separately in Dockerfile with the correct CUDA/CPU index URL.
# Do NOT add torch here — it would be reinstalled as CPU-only from PyPI.
tqdm>=4.66.1
ftfy>=6.1.1
rapidfuzz>=3.8.1
# surya-ocr 0.17.x requires transformers 4.x — transformers 5.0 removed APIs surya depends on.
# pyproject.toml already declares "^4.45.2" (<5.0), so we enforce the same here.
transformers>=4.45.2,<5.0.0
surya-ocr==0.17.1
regex>=2024.4.28
pdftext~=0.6.3
markdownify>=1.1.0
click>=8.2.0
markdown2>=2.5.2
filetype>=1.2.0
# ERP import mode reads backend/erp/schema.yaml (also a transitive dep of
# transformers/ultralytics, but this path depends on it directly).
PyYAML>=6.0
google-genai>=1.0.0
anthropic>=0.46.0
scikit-learn>=1.6.1
openai>=1.65.2

# YOLO Notes region detection (AGPL-3.0 — commercial use requires an
# Ultralytics Enterprise License). Pulls in torchvision.
ultralytics>=8.3.0

# fastdoc office parsers. Small pure-Python packages; the PDF fast path does
# not need them (it runs on pdftext, already required above). Without these,
# /api/convert-fast still serves PDF/CSV/TXT and reports a clear error for
# the office formats instead of failing obscurely.
python-docx>=1.1.0
openpyxl>=3.1.5
python-pptx>=1.0.2
